define([
    'router',
    'backbone',
    'bootstrap',
    'jquery.serialize-object',
    'jquery.color'
], function (Router) {
    "use strict";

    var initialize = function () {
        Router.initialize();
    }

    return {
        initialize: initialize
    };
});
